<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-tools-busybox" role="wrap">
  <?dbhtml filename="busybox.html"?>

  <sect1info condition="script">
    <productname>busybox</productname>
    <productnumber>&busybox-version;</productnumber>
    <address>&busybox-url;</address>
  </sect1info>

  <title>BusyBox-&busybox-version;</title>

  <indexterm zone="ch-tools-busybox">
    <primary sortas="a-BusyBox">BusyBox</primary>
    <secondary>tools</secondary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
    href="../chapter06/busybox.xml"
    xpointer="xpointer(/sect1/sect2[1]/para[1])"/>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&busybox-ch5-sbu;</seg>
        <seg>&busybox-ch5-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installation of BusyBox</title>

    <para>Prepare BusyBox for compilation:</para>

<screen><userinput remap="configure">make defconfig</userinput></screen>

    <para>Compile the package:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Install the package:</para>

<!--<screen><userinput remap="install">make CONFIG_PREFIX=/tools -j1 install</userinput></screen>-->
<screen><userinput remap="install">install -v -m 755 busybox /tools/bin
for program in addgroup adduser ash base64 basename blockdev bunzip2 bzcat
bzip2 cal cat chgrp chmod chown chpasswd chroot chrt cksum cmp comm cp cpio cut
date dc dd delgroup deluser depmod df diff dirname dmesg dnsdomainname du echo
egrep eject env expand expr false fdformat fdisk fgrep find flock fold free
fstrim fuser getopt getty grep groups gunzip gzip halt hd head hexdump hostid
hostname hwclock id ifconfig init insmod install ionice iostat ip ipcrm ipcs
kill killall killall5 klogd last linux32 linux64 ln logger login logname
losetup ls lsmod lzcat lzma md5sum mdev mkdir mkfifo mknod mkswap mktemp
modinfo modprobe more mount mountpoint mpstat mv netstat nice nohup nslookup od
passwd patch pgrep pidof ping ping6 pivot_root pkill pmap poweroff printenv
printf ps pstree pwd pwdx readlink readprofile realpath reboot renice rev rm
rmdir rmmod route rtcwake runlevel script scriptreplay sed seq setarch setsid
sh sha1sum sha256sum sha3sum sha512sum shuf sleep sort split stat stty su
sulogin sum swapoff swapon switch_root sync sysctl syslogd tac tail tar tee
telnet test tftp time timeout top touch tr traceroute traceroute6 true tty
umount uname unexpand uniq unlink unlzma unxz uptime users uudecode uuencode vi
vlock wall watch wc wget which who whoami xargs xz xzcat yes zcat ; do ln -sv busybox /tools/bin/$program ; done</userinput></screen>

  </sect2>

</sect1>

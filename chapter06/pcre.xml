<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-pcre" role="wrap">
  <?dbhtml filename="pcre.html"?>

  <sect1info condition="script">
    <productname>pcre</productname>
    <productnumber>&pcre-version;</productnumber>
    <address>&pcre-url;</address>
  </sect1info>

  <title>PCRE-&pcre-version;</title>

  <indexterm zone="ch-system-pcre">
    <primary sortas="a-PCRE">PCRE</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>The PCRE package contains Perl Compatible Regular Expression
    libraries. These are useful for implementing regular expression pattern
    matching using the same syntax and semantics as Perl 5.</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&pcre-ch6-sbu;</seg>
        <seg>&pcre-ch6-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installation of PCRE</title>

    <para>Prepare PCRE for compilation:</para>

<screen><userinput remap="configure">./configure --prefix=/usr \
            --disable-static \
            --enable-unicode-properties \
            --enable-pcre16 \
            --enable-pcre32 \
            --enable-pcregrep-libz \
            --enable-pcregrep-libbz2 \
            --enable-pcretest-libreadline</userinput></screen>

    <variablelist>
      <title>The meaning of the new configure options:</title>

      <varlistentry>
        <term><parameter>--enable-unicode-properties</parameter></term>
        <listitem>
          <para>This parameter enables Unicode properties support and includes
          the code for handling UTF-8/16/32 character strings in the library.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-pcre16</parameter></term>
        <listitem>
          <para>This parameter enables 16 bit character support.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-pcre32</parameter></term>
        <listitem>
          <para>This parameter enables 32 bit character support.</para>
        </listitem>
      </varlistentry>
      
      <varlistentry>
        <term><parameter>--enable-pcregrep-libz</parameter></term>
        <listitem>
          <para>This parameter adds support to <command>pcregrep</command> to
          read .gz compressed files. </para>
        </listitem>
      </varlistentry>
      
      <varlistentry>
        <term><parameter>--enable-pcregrep-libbz2</parameter></term>
        <listitem>
          <para>This parameter adds support to <command>pcregrep</command> to
          read .bz2 compressed files.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-pcretest-libreadline</parameter></term>
        <listitem>
          <para>This parameter adds line editing and history features to
          <command>pcretest</command> program.</para>
        </listitem>
      </varlistentry>

    </variablelist>

    <para>Compile the package:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Install the package:</para>

<screen><userinput remap="install">make install</userinput></screen>

    <para>The shared library needs to be moved to
    <filename class="directory">/lib</filename>, and as a result the
    <filename class="extension">.so</filename> file in
    <filename class="directory">/usr/lib</filename> will need to be recreated:</para>

<screen><userinput remap="install">mv -v /usr/lib/libpcre.so.* /lib
ln -sfv ../../lib/$(readlink /usr/lib/libpcre.so) /usr/lib/libpcre.so</userinput></screen>

  </sect2>

  <sect2 id="contents-pcre" role="content">
    <title>Contents of PCRE</title>

    <segmentedlist>
      <segtitle>Installed programs</segtitle>
      <segtitle>Installed libraries</segtitle>
      <segtitle>Installed directory</segtitle>

      <seglistitem>
        <seg>pcre-config, pcregrep, and pcretest</seg>
        <seg>libpcre16.so, libpcre32.so, libpcrecpp.so, libpcreposix.so, and libpcre.so</seg>
        <seg>/usr/share/doc/pcre</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Short Descriptions</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="pcre-config">
        <term><command>pcre-config</command></term>
        <listitem>
          <para>Used during the compile process of programs linking to the PCRE libraries.</para>
          <indexterm zone="ch-system-pcre pcre-config">
            <primary sortas="b-pcre-config">pcre-config</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="pcregrep">
        <term><command>pcregrep</command></term>
        <listitem>
          <para><command>grep</command> that understands Perl compatible regular expressions.</para>
          <indexterm zone="ch-system-pcre pcregrep">
            <primary sortas="b-pcregrep">pcregrep</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="pcretest">
        <term><command>pcretest</command></term>
        <listitem>
          <para>Can test a Perl compatible regular expression.</para>
          <indexterm zone="ch-system-pcre pcretest">
            <primary sortas="b-pcretest">pcretest</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>

<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

]>

<sect1 id="hostname" xreflabel="Configuring the system hostname">
  <?dbhtml filename="hostname.html"?>

  <title>Configuring the system hostname</title>

  <para>
    Part of the job of the rc.boot script is setting the system's hostname.
    This needs to be configured in the /etc/hostname file.
  </para>

  <para>
    Create the /etc/hostname file and enter a hostname by running:
  </para>

<screen><userinput>echo "<replaceable>&lt;lfs&gt;</replaceable>" &gt; /etc/hostname</userinput></screen>

  <para>
    <replaceable>&lt;lfs&gt;</replaceable> needs to be replaced with the name
    given to the computer. Do not enter the Fully Qualified Domain Name (FQDN)
    here. That information is put in the /etc/hosts file.
  </para>

</sect1>

<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY xorg-server-homepage      "http://www.x.org/">
  <!ENTITY xorg-server-wikipedia     "https://en.wikipedia.org/wiki/X.Org_Server">
  <!ENTITY xorg-server-download-http "&xorg-download-http;/xserver/xorg-server-&xorg-server-version;.tar.bz2">
  <!ENTITY xorg-server-download-ftp  "&xorg-download-ftp;/xserver/xorg-server-&xorg-server-version;.tar.bz2">
  <!ENTITY xorg-server-md5sum        "3c1c1057d3ad27380d8dd87ffcc182cd">
  <!ENTITY xorg-server-size          "5.6 MB">
  <!ENTITY xorg-server-buildsize     "516 MB">
  <!ENTITY xorg-server-time          "1.6 SBU">
]>

<sect1 id="xorg-server" xreflabel="xorg-server-&xorg-server-version;">
  <?dbhtml filename="xorg-server.html"?>

  <title>xorg-server-&xorg-server-version;</title>

  <sect2 role="package">
    <title>Introduction</title>

    <para>
      The Xorg Server is the core of the X Window system.
    </para>

    <bridgehead renderas="sect3">Package Information</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Homepage: <ulink url="&xorg-server-homepage;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Wikipedia: <ulink url="&xorg-server-wikipedia;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (HTTP): <ulink url="&xorg-server-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (FTP): <ulink url="&xorg-server-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download MD5 sum: &xorg-server-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Download size: &xorg-server-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated disk space required: &xorg-server-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated build time: &xorg-server-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependencies</bridgehead>

    <bridgehead renderas="sect4">Required</bridgehead>
    <para>
      <xref linkend="pixman"/>,
      <xref linkend="libpciaccess"/>,
      <xref linkend="libdrm"/>,
      <xref linkend="libressl"/>,
      <xref linkend="fixesproto"/>,
      <xref linkend="libxkbfile"/>,
      <xref linkend="damageproto"/>,
      <xref linkend="xcmiscproto"/>,
      <xref linkend="bigreqsproto"/>,
      <xref linkend="randrproto"/>,
      <xref linkend="renderproto"/>,
      <xref linkend="libxfont"/>,
      <xref linkend="videoproto"/>,
      <xref linkend="resourceproto"/>, and
      <xref linkend="xineramaproto"/>
    </para>

    <bridgehead renderas="sect4">Recommended</bridgehead>
    <para>
      <xref linkend="mesa"/>,
      <xref linkend="compositeproto"/>,
      <xref linkend="recordproto"/>,
      <xref linkend="scrnsaverproto"/>, and
      <xref linkend="presentproto"/>
    </para>

    <bridgehead renderas="sect4">Optional</bridgehead>
    <para>
      <xref linkend="xf86driproto"/>,
      <xref linkend="xf86bigfontproto"/>, and
      <xref linkend="xf86dgaproto"/>
    </para>

    <bridgehead renderas="sect4">Required (runtime)</bridgehead>
    <para>
      <xref linkend="xorg7-driver"/>,
      <xref linkend="x-fonts"/>,
      <xref linkend="xkbcomp"/> and
      <xref linkend="xkeyboard-config"/>
    </para>

  </sect2>

  <sect2>
    <title>Installation</title>

    <para>
      Prepare the package for compilation:
    </para>

<screen><userinput>./configure --prefix= \
            --with-xkb-output=/var/lib/xkb \
            --with-default-font-path=/share/fonts \
            --enable-install-setuid \
            --enable-suid-wrapper \
            --with-sha1=libcrypto \
            --disable-xnest \
            --disable-dri</userinput></screen>

    <para>
      Compile the package:
    </para>

<screen><userinput>make</userinput></screen>

    <para>
      Install the package:
    </para>

<screen role="root"><userinput>porg -lD "make -j1 install" &amp;&amp;
mkdir -pv /etc/X11/xorg.conf.d &amp;&amp;
cat &gt;&gt; /etc/sysconfig/createfiles &lt;&lt; "EOF"
<literal>/tmp/.ICE-unix dir 1777 root root
/tmp/.X11-unix dir 1777 root root</literal>
EOF</userinput></screen>

    <sect3>
      <title>Command Explanations</title>

      <para>
        <parameter>--enable-install-setuid</parameter>: The Xorg binary must
        run as the root user. This switch ensures that the binary is installed
        setuid when <command>make</command> is run by an unprivileged user.
      </para>

      <para>
        <parameter>--enable-suid-wrapper</parameter>: Build suid-root wrapper
        for legacy driver support on rootless xserver systems.
      </para>

      <para>
        <parameter>--with-sha1=libcrypto</parameter>: This switch forces using
        LibreSSL SHA1 implementation.
      </para>

      <para>
        <parameter>--disable-xnest</parameter>: This switch disables building
        Xnest server.
      </para>

      <para>
        <parameter>--disable-dri</parameter>: This switch disables bulding DRI
        extension.
      </para>

      <para>
        <option>--disable-glx</option>: This switch disables building GLX
        extension.
      </para>

      <para>
        <option>--disable-composite</option>: This switch disables building
        Composite extension.
      </para>

      <para>
        <option>--disable-record</option>: This switch disables building
        Record extension.
      </para>

      <para>
        <option>--disable-screensaver</option>: This switch disables building
        ScreenSaver extension.
      </para>

      <para>
        <option>--disable-present</option>: This switch disables building
        Present extension.
      </para>

      <para>
        <command>cat &gt;&gt; /etc/sysconfig/createfiles...</command>: This
        command creates the <filename class="directory">/tmp/.ICE-unix</filename>
        and <filename class="directory">/tmp/.X11-unix</filename> directories at
        startup, and ensures that the permissions and ownership are correct as
        required by the server.
      </para>

    </sect3>

  </sect2>

  <sect2>
    <title>Configuration</title>

<screen role="root"><userinput>cat &gt;&gt; /etc/X11/xorg.conf.d/00-disable-hotplug.conf &lt;&lt; "EOF"
<literal>Section "ServerFlags"
        option "AutoAddDevices" "False"
EndSection</literal>
EOF
ls /etc/X11/xorg.conf.d/00-disable-hotplug.conf | porg -lD+</userinput></screen>

  </sect2>

</sect1>

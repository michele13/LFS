<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY firefox-homepage      "https://www.mozilla.org/firefox/">
  <!ENTITY firefox-wikipedia     "https://en.wikipedia.org/wiki/Firefox">
  <!ENTITY firefox-download-http "&mozilla-http;/firefox/releases/&firefox-version;/source/firefox-&firefox-version;.source.tar.xz">
  <!ENTITY firefox-download-ftp  "&mozilla-ftp;/firefox/releases/&firefox-version;/source/firefox-&firefox-version;.source.tar.xz">
  <!ENTITY firefox-md5sum        "ea1e436b278b933747d9de167310592c">
  <!ENTITY firefox-size          "176 MB">
  <!ENTITY firefox-buildsize     "3.6 GB">
  <!ENTITY firefox-time          "29 SBU">
]>

<sect1 id="firefox" xreflabel="firefox-&firefox-version;">
  <?dbhtml filename="firefox.html" ?>

  <title>firefox-&firefox-version;</title>

  <sect2 role="package">
    <title>Introduction</title>

    <para>
      Firefox is a stand-alone browser based on the Mozilla codebase.
    </para>

    <bridgehead renderas="sect3">Package Information</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Homepage: <ulink url="&firefox-homepage;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Wikipedia: <ulink url="&firefox-wikipedia;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (HTTP): <ulink url="&firefox-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (FTP): <ulink url="&firefox-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download MD5 sum: &firefox-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Download size: &firefox-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated disk space required: &firefox-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated build time: &firefox-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependencies</bridgehead>

    <bridgehead renderas="sect4">Required</bridgehead>
    <para>
      <xref linkend="libxt"/>,
      <xref linkend="gtk"/>,
      <xref linkend="alsa-lib"/>,
      <xref linkend="unzip"/>, and
      <xref linkend="zip"/>
    </para>

    <bridgehead renderas="sect4">Recommended</bridgehead>
    <para>
      <xref linkend="ffmpeg"/> (runtime),
      <xref linkend="libwebp"/>,
      <xref linkend="icu"/>,
      <xref linkend="libevent"/>,
      <xref linkend="libvpx"/>,
      <xref linkend="nspr"/>,
      <xref linkend="nss"/>,
      <xref linkend="sqlite"/>,
      <xref linkend="yasm"/>, and
      <ulink url="https://github.com/libproxy/libproxy">libproxy</ulink>
    </para>

    <note>
      <para>
        If you don't install recommended dependencies, then internal copies of
        those packages will be used. They might be tested to work, but they can
        be out of date or contain security holes.
      </para>
    </note>

    <note>
      <para>
        With Firefox-31.0 and later versions, you must have installed libressl
        before python2, or the build system will quickly fail with output
        including "ImportError: cannot import name HTTPSHandler". If you are in
        any doubt about this (e.g. upgrading from an older version of Firefox),
        check if <filename>/usr/lib/python2.7/lib-dynload/_ssl.so</filename>
        exists.  If it does not, reinstall <xref linkend="python2"/> (after
        installing <xref linkend="libressl"/> - the latest version of any
        <emphasis>currently maintained</emphasis> version of Openssl should be
        satisfactory if already installed - if that package has not already
        been installed).
      </para>
    </note>

    <bridgehead renderas="sect4">Optional</bridgehead>
    <para>
      <xref linkend="curl"/>,
      <xref linkend="hunspell"/>, and
      <ulink url="http://sourceforge.net/projects/liboauth/files/">liboauth</ulink>
    </para>

  </sect2>

  <sect2>
    <title>Installation</title>

    <para>
      The configuration of Firefox is accomplished by creating a
      <filename>mozconfig</filename> file containing the desired configuration
      options. A default <filename>mozconfig</filename> is created below. To
      see the entire list of available configuration options (and an
      abbreviated description of each one), issue <command>./configure
      --help</command>. You may also wish to review the entire file and
      uncomment any other desired options. Create the file by issuing the
      following command:
    </para>

<screen><userinput>cat &gt; mozconfig &lt;&lt; "EOF"
<literal># Uncomment these lines if you have installed optional dependencies:
#ac_add_options --enable-system-hunspell

# If you have not installed Yasm then uncomment this line:
#ac_add_options --disable-webm

# Comment out following options if you have not installed
# recommended dependencies:
ac_add_options --enable-system-sqlite
ac_add_options --with-system-libevent
ac_add_options --with-system-libvpx
ac_add_options --with-system-nspr
ac_add_options --with-system-nss
ac_add_options --with-system-icu

# The BLFS editors recommend not changing anything below this line:
ac_add_options --prefix=
ac_add_options --enable-application=browser

ac_add_options --disable-crashreporter
ac_add_options --disable-updater
ac_add_options --disable-tests

ac_add_options --enable-optimize
ac_add_options --enable-strip
ac_add_options --enable-install-strip

ac_add_options --enable-gio
ac_add_options --enable-official-branding
ac_add_options --enable-safe-browsing
ac_add_options --enable-url-classifier

ac_add_options --disable-dbus
ac_add_options --disable-necko-wifi
ac_add_options --disable-gconf
ac_add_options --disable-libnotify
ac_add_options --disable-pulseaudio
ac_add_options --disable-gstreamer

# From firefox-40, using system cairo causes firefox to crash
# frequently when it is doing background rendering in a tab.
#ac_add_options --enable-system-cairo
ac_add_options --enable-system-ffi
ac_add_options --enable-system-pixman

ac_add_options --with-pthreads

ac_add_options --with-system-bz2
ac_add_options --with-system-jpeg
ac_add_options --with-system-png
ac_add_options --with-system-zlib

mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/firefox-build-dir</literal>
EOF</userinput></screen>

    <para>
      Compile the package:
    </para>

<screen><userinput>SHELL=/bin/sh make -f client.mk</userinput></screen>

    <para>
      Install the package:
    </para>

<screen role="root"><userinput>porg -lD -E $PWD "SHELL=/bin/sh make -f client.mk install INSTALL_SDK= &amp;&amp;
chown -R 0:0 /lib/firefox-&firefox-version; &amp;&amp;
mkdir -pv /lib/mozilla/plugins &amp;&amp;
ln -sfv ../../mozilla/plugins /lib/firefox-&firefox-version;/browser"</userinput></screen>

    <sect3>
      <title>Command Explanations</title>

      <para>
        <command>make -f client.mk ...</command>: Mozilla products are packaged
        to allow the use of a configuration file which can be used to pass the
        configuration settings to the <command>configure</command> command.
        <command>make</command> uses the <filename>client.mk</filename> file to
        get initial configuration and setup parameters.
      </para>

      <para>
        <command>mkdir -pv /lib/mozilla/plugins</command>: This checks that
        <filename class="directory">/lib/mozilla/plugins</filename> exists.
      </para>

      <para>
        <command>ln -sv ... /lib/firefox-&firefox-version;/browser</command>:
        This command creates a symbolic link to <filename
        class="directory">/lib/mozilla/plugins</filename>. It's not really
        needed, as Firefox checks <filename
        class="directory">/lib/mozilla/plugins</filename> by default, but the
        symbolic link is made to keep all the plugins installed in one folder.
      </para>

    </sect3>

  </sect2>

</sect1>

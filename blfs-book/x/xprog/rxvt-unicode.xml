<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY rxvt-unicode-homepage      "http://software.schmorp.de/pkg/rxvt-unicode.html">
  <!ENTITY rxvt-unicode-wikipedia     "http://en.wikipedia.org/wiki/Rxvt-unicode">
  <!ENTITY rxvt-unicode-download-http "http://dist.schmorp.de/rxvt-unicode/Attic/rxvt-unicode-&rxvt-unicode-version;.tar.bz2">
  <!ENTITY rxvt-unicode-download-ftp  " ">
  <!ENTITY rxvt-unicode-md5sum        "a9a06c608258c5fd247c3725d8f44582">
  <!ENTITY rxvt-unicode-size          "904 KB">
  <!ENTITY rxvt-unicode-buildsize     "26 MB">
  <!ENTITY rxvt-unicode-time          "0.2 SBU">
]>

<sect1 id="rxvt-unicode" xreflabel="rxvt-unicode-&rxvt-unicode-version;">
  <?dbhtml filename="rxvt-unicode.html"?>

  <title>rxvt-unicode-&rxvt-unicode-version;</title>

  <sect2 role="package">
    <title>Introduction</title>

    <para>
      rxvt-unicode is a modern, Unicode-aware color xterm replacement that uses
      significantly less memory than a conventional xterm and many other Unicode
      supporting terminal emulators. It supports using multiple fonts at the
      same time, including Xft fonts, and client-server technology to reduce
      memory consumption when using multiple windows.
    </para>

    <bridgehead renderas="sect3">Package Information</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Homepage: <ulink url="&rxvt-unicode-homepage;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Wikipedia: <ulink url="&rxvt-unicode-wikipedia;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (HTTP): <ulink url="&rxvt-unicode-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (FTP): <ulink url="&rxvt-unicode-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download MD5 sum: &rxvt-unicode-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Download size: &rxvt-unicode-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated disk space required: &rxvt-unicode-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated build time: &rxvt-unicode-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependencies</bridgehead>

    <bridgehead renderas="sect4">Required</bridgehead>
    <para>
      <xref linkend="libx11"/>
    </para>

    <bridgehead renderas="sect4">Recommended</bridgehead>
    <para>
      <xref linkend="libxft"/>
    </para>

    <bridgehead renderas="sect4">Optional</bridgehead>
    <para>
      <xref linkend="libxrender"/> (for simulated transparency), and
      <xref linkend="gdk-pixbuf"/> (for background images)
    </para>

  </sect2>

  <sect2>
    <title>Installation</title>

    <para>
      Prepare the package for compilation:
    </para>

<screen><userinput>./configure --prefix= \
            --enable-everything</userinput></screen>

    <para>
      Compile the package:
    </para>

<screen><userinput>make</userinput></screen>

    <para>
      Install the package:
    </para>

<screen role="root"><userinput>porg -lD -E $PWD "make -j1 install"</userinput></screen>

    <sect3>
      <title>Command Explanations</title>

      <para>
        <parameter>--enable-everything</parameter>: Add support for all
        non-multichoice options. Details about the different options can be
        found in the file <filename>README.configure</filename>.
      </para>

      <para>
        <option>--disable-xft</option>: Remove support for Xft fonts.
      </para>

      <para>
        <option>--disable-perl</option>: Disable the embedded Perl interpreter.
      </para>

    </sect3>

  </sect2>

  <sect2>
    <title>Configuration</title>

    <para>
      The rxvt-unicode terminal emulator uses the resource class
      <classname>URxvt</classname> and the resource name
      <classname>urxvt</classname>. You can add X resource definitions to a
      user's <filename>~/.Xresources</filename> file or to the system-wide
      <filename>/etc/X11/app-defaults/URxvt</filename> file. The following
      example will load the <classname>matcher</classname> Perl extension
      (assuming Perl support wasn't disabled), which enables a middle button
      click to open an underlined URL in the specified browser, sets a
      background and foreground color and loads an Xft font:
    </para>

<screen><userinput>cat &gt;&gt; /etc/X11/app-defaults/URxvt &lt;&lt; "EOF"
<literal>URxvt*perl-ext: matcher
URxvt*urlLauncher: firefox
URxvt.background: black
URxvt.foreground: yellow
URxvt*font: xft:Monospace:pixelsize=12</literal>
EOF</userinput></screen>

    <para>
      The rxvt-unicode application can also run in a daemon mode, which makes it
      possible to open multiple terminal windows within the same process. The
      <command>urxvtc</command> client then connects to the
      <command>urxvtd</command> daemon and requests a new terminal window. Use
      this option with caution. If the daemon crashes, all the running processes
      in the terminal windows are terminated.
    </para>

    <para>
      You can start the <command>urxvtd</command> daemon in the system or
      personal startup <command>X</command> session script (e.g.,
      <filename>~/.xinitrc</filename>) by adding the following lines near the
      top of the script:
    </para>

<screen><userinput><literal># Start the urxvtd daemon
urxvtd -q -f -o &amp;</literal></userinput></screen>

    <para>
      For more information, examine the <command>urxvt</command>,
      <command>urxvtd</command>, <command>urxvtc</command>, and
      <filename>urxvtperl</filename> man pages.
    </para>

  </sect2>

</sect1>

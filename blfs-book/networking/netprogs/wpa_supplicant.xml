<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY wpa_supplicant-homepage      "https://w1.fi/wpa_supplicant/">
  <!ENTITY wpa_supplicant-wikipedia     "https://en.wikipedia.org/wiki/Wpa_supplicant">
  <!ENTITY wpa_supplicant-download-http "http://hostap.epitest.fi/releases/wpa_supplicant-&wpa_supplicant-version;.tar.gz">
  <!ENTITY wpa_supplicant-download-ftp  " ">
  <!ENTITY wpa_supplicant-md5sum        "96ff75c3a514f1f324560a2376f13110">
  <!ENTITY wpa_supplicant-size          "2.5 MB">
  <!ENTITY wpa_supplicant-buildsize     "31 MB">
  <!ENTITY wpa_supplicant-time          "0.4 SBU">
]>

<sect1 id="wpa_supplicant" xreflabel="wpa_supplicant-&wpa_supplicant-version;">
  <?dbhtml filename="wpa_supplicant.html"?>

  <title>wpa_supplicant-&wpa_supplicant-version;</title>

  <sect2 role="package">
    <title>Introduction</title>

    <para>
      WPA Supplicant is a Wi-Fi Protected Access (WPA) client and IEEE 802.1X
      supplicant. It implements WPA key negotiation with a WPA Authenticator
      and Extensible Authentication Protocol (EAP) authentication with an
      Authentication Server. In addition, it controls the roaming and IEEE
      802.11 authentication/association of the wireless LAN driver. This is
      useful for connecting to a password protected wireless access point.
    </para>

    <bridgehead renderas="sect3">Package Information</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Homepage: <ulink url="&wpa_supplicant-homepage;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Wikipedia: <ulink url="&wpa_supplicant-wikipedia;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (HTTP): <ulink url="&wpa_supplicant-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (FTP): <ulink url="&wpa_supplicant-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download MD5 sum: &wpa_supplicant-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Download size: &wpa_supplicant-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated disk space required: &wpa_supplicant-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated build time: &wpa_supplicant-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependencies</bridgehead>

    <bridgehead renderas="sect4">Recommended</bridgehead>
    <para>
      <xref linkend="libnl"/>,
      <xref linkend="libressl"/>, and
      <xref linkend="readline"/>
    </para>

  </sect2>

  <sect2>
    <title>Kernel Configuration</title>

    <para>
      Enable the following options in the kernel configuration as well as
      specific device drivers for your hardware and recompile the kernel if
      necessary:
    </para>

<screen><literal>[*] Networking support  ---&gt;                              [CONFIG_NET]
  [*] Wireless  ---&gt;                                      [CONFIG_WIRELESS]
    &lt;*/M&gt; cfg80211 - wireless configuration API           [CONFIG_CFG80211]
    [*]     cfg80211 wireless extensions compatibility    [CONFIG_CFG80211_WEXT]
    &lt;*/M&gt; Generic IEEE 802.11 Networking Stack (mac80211) [CONFIG_MAC80211]
Device Drivers  ---&gt;
  [*] Network device support  ---&gt;                        [CONFIG_NETDEVICES]
    [*] Wireless LAN  ---&gt;                                [CONFIG_WLAN]</literal></screen>

    <para>
      Open the submenu and select the options that support your hardware:
      <command>lspci</command> from <xref linkend="pciutils"/> can be used to
      view your hardware configuration.
    </para>

  </sect2>

  <sect2>
    <title>Installation</title>

    <para>
      First you will need to create an initial configuration file for the
      build process. You can read <filename>wpa_supplicant/README</filename>
      and <filename>wpa_supplicant/defconfig</filename> for the explanation
      of the following options as well as other options that can be used.
      Create a build configuration file that should work for standard WiFi
      setups by running the following command:
    </para>

<screen><userinput>cat &gt; wpa_supplicant/.config &lt;&lt; "EOF"
<literal>CONFIG_BACKEND=file
CONFIG_CTRL_IFACE=y
CONFIG_DEBUG_FILE=y
CONFIG_DEBUG_SYSLOG=y
CONFIG_DEBUG_SYSLOG_FACILITY=LOG_DAEMON
CONFIG_DRIVER_NL80211=y
CONFIG_DRIVER_WEXT=y
CONFIG_DRIVER_WIRED=y
CONFIG_EAP_GTC=y
CONFIG_EAP_LEAP=y
CONFIG_EAP_MD5=y
CONFIG_EAP_MSCHAPV2=y
CONFIG_EAP_OTP=y
CONFIG_EAP_PEAP=y
CONFIG_EAP_TLS=y
CONFIG_EAP_TTLS=y
CONFIG_IEEE8021X_EAPOL=y
CONFIG_IPV6=y
CONFIG_LIBNL32=y
CONFIG_PEERKEY=y
CONFIG_PKCS12=y
CONFIG_READLINE=y
CONFIG_SMARTCARD=y
CONFIG_WPS=y
CFLAGS += -I/include/libnl3</literal>
EOF</userinput></screen>

    <para>
      Compile the package:
    </para>

<screen><userinput>cd wpa_supplicant &amp;&amp;
make BINDIR=/bin LIBDIR=/lib</userinput></screen>

    <para>
      Instal the package:
    </para>

<screen role="root"><userinput>porg -lp wpa_supplicant-&wpa_supplicant-version; "install -v -m755 wpa_cli wpa_passphrase wpa_supplicant /bin &amp;&amp;
install -v -m644 doc/docbook/wpa_supplicant.conf.5 /share/man/man5 &amp;&amp;
install -v -m644 doc/docbook/wpa_cli.8 doc/docbook/wpa_passphrase.8 doc/docbook/wpa_supplicant.8 /share/man/man8"</userinput></screen>

  </sect2>

  <sect2>
    <title>Configuration</title>

    <para>
      To connect to an access point that uses a password, you need to put the
      pre-shared key in <filename>
      /etc/sysconfig/wpa_supplicant-<replaceable>wlan0</replaceable>.conf</filename>.
      SSID is the string that the access point/router transmits to identify
      itself. Run the following command as the <systemitem
      class="username">root</systemitem> user:
    </para>

<screen role="root"><userinput>wpa_passphrase <replaceable>SSID</replaceable> <replaceable>SECRET_PASSWORD</replaceable> &gt; /etc/sysconfig/wpa_supplicant-<replaceable>wlan0</replaceable>.conf</userinput></screen>

    <para>
      <filename>/etc/sysconfig/wpa_supplicant-<replaceable>wlan0</replaceable>.conf
      </filename> can hold the details of several access points. When
      <command>wpa_supplicant</command> is started, it will scan for the SSIDs
      it can see and choose the appropriate password to connect.
    </para>

    <para>
      If you want to connect to an access point that isn't password protected,
      put an entry like this in <filename> /etc/sysconfig/wpa_supplicant-<replaceable>wlan0</replaceable>.conf</filename>.
      Replace "Some-SSID" with the SSID of the access point/router.
    </para>

<screen>network={
  ssid="<replaceable>Some-SSID</replaceable>"
  key_mgmt=NONE
}</screen>

    <para>
      There are many options that you could use to tweak how you connect
      to each access point. They are described in some detail in the
      <filename>wpa_supplicant/wpa_supplicant.conf</filename> file in the
      source tree.
    </para>

    <sect3>
      <title>Connecting to an Access Point</title>

      <para id="wpa-service">
        If you want to configure network interfaces at boot using
        <command>wpa_supplicant</command>, you need to install the
        <filename>/lib/services/wpa</filename> script
        included in <xref linkend="bootscripts"/> package:
      </para>

<screen role="root"><userinput>porg -lp+ wpa_supplicant-&wpa_supplicant-version; "make -j1 install-service-wpa"</userinput></screen>

      <para>
        If your router/access point uses DHCP to allocate IP addresses, you can
        install <xref linkend="dhcp"/> client and use it to automatically obtain
        network addresses. Create the <filename>/etc/sysconfig/ifconfig-<replaceable>wlan0</replaceable>
        </filename> by running the following command as the <systemitem
        class="username">root</systemitem> user:
      </para>

<screen role="root"><userinput>cat &gt; /etc/sysconfig/ifconfig.<replaceable>wlan0</replaceable> &lt;&lt; "EOF"
<literal>ONBOOT="yes"
IFACE="<replaceable>wlan0</replaceable>"
SERVICE="wpa"

# Additional arguments to wpa_supplicant
WPA_ARGS=""

WPA_SERVICE="dhclient"
DHCP_START=""
DHCP_STOP=""

# Set PRINTIP="yes" to have the script print
# the DHCP assigned IP address
PRINTIP="no"

# Set PRINTALL="yes" to print the DHCP assigned values for
# IP, SM, DG, and 1st NS. This requires PRINTIP="yes".
PRINTALL="no"</literal>
EOF</userinput></screen>

      <para>
        If you prefer <xref linkend="dhcpcd"/> instead of <xref linkend="dhcp"/>
        client, then create the <filename>/etc/sysconfig/ifconfig-<replaceable>wlan0</replaceable>
        </filename> by running the following command as the <systemitem
        class="username">root</systemitem> user:
      </para>

<screen role="root"><userinput>cat &gt; /etc/sysconfig/ifconfig.<replaceable>wlan0</replaceable> &lt;&lt; "EOF"
<literal>ONBOOT="yes"
IFACE="<replaceable>wlan0</replaceable>"
SERVICE="wpa"

# Additional arguments to wpa_supplicant
WPA_ARGS=""

WPA_SERVICE="dhcpcd"
DHCP_START="-b -q <replaceable>&lt;insert appropriate start options here&gt;</replaceable>"
DHCP_STOP="-k <replaceable>&lt;insert additional stop options here&gt;</replaceable>"</literal>
EOF</userinput></screen>

      <para>
        Alternatively, if you use static addresses on your local network, then
        create the <filename>/etc/sysconfig/ifconfig-<replaceable>wlan0</replaceable>
        </filename> by running the following command as the <systemitem
        class="username">root</systemitem> user:
      </para>

<screen role="root"><userinput>cat &gt; /etc/sysconfig/ifconfig.<replaceable>wlan0</replaceable> &lt;&lt; "EOF"
<literal>ONBOOT="yes"
IFACE="<replaceable>wlan0</replaceable>"
SERVICE="wpa"

# Additional arguments to wpa_supplicant
WPA_ARGS=""

WPA_SERVICE="ipv4-static"
IP="192.168.1.1"
GATEWAY="192.168.1.2"
PREFIX="24"
BROADCAST="192.168.1.255"</literal>
EOF</userinput></screen>

      <para>
        You can connect to the wireless access point by running the following
        command as the <systemitem class="username">root</systemitem> user:
      </para>

<screen role="root"><userinput>ifup <replaceable>wlan0</replaceable></userinput></screen>

      <para>
        Replace <replaceable>wlan0</replaceable> with the correct wireless
        interface and <replaceable>wlan0</replaceable> with desired name for the
        configuration file. Please note that <filename>wpa_supplicant-*.conf</filename>
        and <filename>ifconfig.*</filename> configuration files need to have
        identical names, ie both contain <replaceable>wlan0</replaceable> in their name.
      </para>

    </sect3>

  </sect2>

</sect1>

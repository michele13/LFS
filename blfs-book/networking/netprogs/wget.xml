<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY wget-homepage      "http://www.gnu.org/software/wget/">
  <!ENTITY wget-wikipedia     "http://en.wikipedia.org/wiki/Wget">
  <!ENTITY wget-download-http "http://ftp.gnu.org/gnu/wget/wget-&wget-version;.tar.xz">
  <!ENTITY wget-download-ftp  "ftp://ftp.gnu.org/gnu/wget/wget-&wget-version;.tar.xz">
  <!ENTITY wget-md5sum        "af9ca95a4bb8ac4a9bf10aeae66fa5ec">
  <!ENTITY wget-size          "1.8 MB">
  <!ENTITY wget-buildsize     "22 MB">
  <!ENTITY wget-time          "0.4 SBU">
]>

<sect1 id="wget" xreflabel="wget-&wget-version;">
  <?dbhtml filename="wget.html"?>

  <title>wget-&wget-version;</title>

  <sect2 role="package">
    <title>Introduction</title>

    <para>
      The wget package contains a utility useful for non-interactive
      downloading of files from the Web.
    </para>

    <bridgehead renderas="sect3">Package Information</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Homepage: <ulink url="&wget-homepage;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Wikipedia: <ulink url="&wget-wikipedia;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (HTTP): <ulink url="&wget-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (FTP): <ulink url="&wget-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download MD5 sum: &wget-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Download size: &wget-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated disk space required: &wget-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated build time: &wget-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependencies</bridgehead>

    <bridgehead renderas="sect4">Recommended</bridgehead>
    <para>
      <xref linkend="libressl"/>
    </para>

    <bridgehead renderas="sect4">Optional</bridgehead>
    <para>
      <!--<xref linkend="libidn"/> and-->
      <xref linkend="pcre"/>
    </para>

  </sect2>

  <sect2>
    <title>Installation</title>

    <para>
      Suppress installation of unnecessary files:
    </para>

<screen><userinput>sed 's/install-info-am$//' -i doc/Makefile.in</userinput></screen>

    <para>
      Prepare the package for compilation:
    </para>

<screen><userinput>./configure --prefix= \
            --disable-nls \
            --with-ssl=openssl</userinput></screen>

    <para>
      Compile the package:
    </para>

<screen><userinput>make</userinput></screen>

    <para>
      Install the package:
    </para>

<screen role="root"><userinput>porg -lp wget-&wget-version; -E $PWD "make -j1 install"</userinput></screen>

    <sect3>
      <title>Command Explanations</title>

      <para>
        <parameter>--with-ssl=openssl</parameter>: This configures the program
        to use LibreSSL instead of GnuTLS which is preferred by the configure
        script.
      </para>

      <para>
        <option>--without-ssl</option>: This configure option is required if
        support for the HTTPS protocol is not needed or LibreSSL is not
        installed.
      </para>

    </sect3>

  </sect2>

</sect1>

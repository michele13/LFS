<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY sudo-homepage      "http://www.sudo.ws/">
  <!ENTITY sudo-wikipedia     "http://en.wikipedia.org/wiki/Sudo">
  <!ENTITY sudo-download-http "http://www.sudo.ws/dist/sudo-&sudo-version;.tar.gz">
  <!ENTITY sudo-download-ftp  "ftp://ftp.sudo.ws/pub/sudo/sudo-&sudo-version;.tar.gz">
  <!ENTITY sudo-md5sum        "50a840a688ceb6fa3ab24fc0adf4fa23">
  <!ENTITY sudo-size          "2.7 MB">
  <!ENTITY sudo-buildsize     "24 MB">
  <!ENTITY sudo-time          "0.4 SBU">
]>

<sect1 id="sudo" xreflabel="sudo-&sudo-version;">
  <?dbhtml filename="sudo.html"?>

  <title>sudo-&sudo-version;</title>

  <sect2 role="package">
    <title>Introduction</title>

    <para>
      The Sudo package allows a system administrator to give certain users (or
      groups of users) the ability to run some (or all) commands as <systemitem
      class="username">root</systemitem> or another user while logging the
      commands and arguments.
    </para>

    <bridgehead renderas="sect3">Package Information</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Homepage: <ulink url="&sudo-homepage;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Wikipedia: <ulink url="&sudo-wikipedia;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (HTTP): <ulink url="&sudo-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (FTP): <ulink url="&sudo-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download MD5 sum: &sudo-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Download size: &sudo-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated disk space required: &sudo-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated build time: &sudo-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependencies</bridgehead>

    <bridgehead renderas="sect4">Recommended</bridgehead>
    <para>
      <xref linkend="mdocml"/> (to correctly build and install man pages)
    </para>

    <bridgehead renderas="sect4">Optional</bridgehead>
    <para>
      <xref linkend="server-mail"/> (that provides a <command>sendmail</command> command),
      <ulink url="http://www.openafs.org/">AFS</ulink>,
      <ulink url="http://www.fwtk.org/">FWTK</ulink>,
      <ulink url="http://sourceforge.net/projects/opie/files/">Opie</ulink> and
      <ulink url="http://www.rsa.com/node.aspx?id=1156">SecurID</ulink>
    </para>

  </sect2>

  <sect2>
    <title>Installation</title>

    <para>
      Suppress installation of unnecessary files:
    </para>

<screen><userinput>sed "s/mkinstalldirs.*/mkinstalldirs \\\/" -i doc/Makefile.in
sed "/DESTDIR.*docdir/d" -i doc/Makefile.in
sed "s/ install-doc//" -i examples/Makefile.in
sed "s/\$(DESTDIR)\$(docdir) //" -i plugins/sudoers/Makefile.in</userinput></screen>

    <para>
      Prepare the package for compilation:
    </para>

<screen><userinput>./configure --prefix= \
            --sbindir=/bin \
            --libexecdir=/lib \
            --disable-nls \
            --with-all-insults \
            --with-env-editor \
            --with-passprompt="[sudo] password for %p: "</userinput></screen>

    <para>
      Compile the package:
    </para>

<screen><userinput>make</userinput></screen>

    <para>
      Install the package:
    </para>

<!-- dev note: make -j1 DESTDIR=<DESTDIR> install_uid=1000 install_gid=1000 install -->

<screen role="root"><userinput>porg -lD -E $PWD "make -j1 install"</userinput></screen>

    <sect3>
      <title>Command Explanations</title>

      <para>
        <parameter>--libexecdir=/lib</parameter>: This switch controls
        where private programs are installed. Everything in that directory is a
        library, so they belong under <filename class="directory">/lib</filename>
        instead of <filename class="directory">/libexec</filename>.
      </para>

      <para>
        <parameter>--with-all-insults</parameter>: This switch includes all the
        Sudo insult sets.
      </para>

      <para>
        <parameter>--with-env-editor</parameter>: This switch enables use of the
        environment variable <envar>EDITOR</envar> for
        <command>visudo</command>.
      </para>

      <note>
        <para>
          There are many options to Sudo's <command>configure</command> command.
          Check the <command>configure --help</command> output for a complete
          list.
        </para>
      </note>

    </sect3>

  </sect2>

  <sect2>
    <title>Configuration</title>

    <para>
      The <filename>sudoers</filename> file can be quite complicated. It is
      composed of two types of entries: aliases (basically variables) and user
      specifications (which specify who may run what). The installation installs
      a default configuration that has no privileges installed for any user.
    </para>

    <para>
      One example usage is to allow the system administrator to execute any
      program without typing a password each time root privileges are needed.
      This can be configured as:
    </para>

<screen><literal># User alias specification
User_Alias  ADMIN = YourLoginId

# Allow people in group ADMIN to run all commands without a password
ADMIN       ALL = NOPASSWD: ALL</literal></screen>

    <para>
      For details, see <command>man sudoers</command>.
    </para>

    <note>
      <para>
        The Sudo developers highly recommend using the <command>visudo</command>
        program to edit the <filename>sudoers</filename> file. This will provide
        basic sanity checking like syntax parsing and file permission to avoid
        some possible mistakes that could lead to a vulnerable configuration.
      </para>
    </note>

  </sect2>

</sect1>

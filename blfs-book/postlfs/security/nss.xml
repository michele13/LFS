<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY nss-url "ftp.mozilla.org/pub/mozilla.org/security/nss/releases">

  <!ENTITY nss-homepage      "https://developer.mozilla.org/en/docs/NSS">
  <!ENTITY nss-wikipedia     "http://en.wikipedia.org/wiki/Network_Security_Services">
  <!ENTITY nss-download-http "https://&nss-url;/NSS_3_23_RTM/src/nss-&nss-version;.tar.gz">
  <!ENTITY nss-download-ftp  "ftp://&nss-url;/NSS_3_23_RTM/src/nss-&nss-version;.tar.gz">
  <!ENTITY nss-md5sum        "21c3fed84441b2ab4c50ac626f6517e7">
  <!ENTITY nss-size          "7.2 MB">
  <!ENTITY nss-buildsize     "84 MB">
  <!ENTITY nss-time          "0.9 SBU">
]>

<sect1 id="nss" xreflabel="nss-&nss-version;">
  <?dbhtml filename="nss.html"?>

  <title>nss-&nss-version;</title>

  <sect2 role="package">
    <title>Introduction</title>

    <para>
      The Network Security Services (NSS) package is a set of libraries designed
      to support cross-platform development of security-enabled client and
      server applications. Applications built with NSS can support SSL v2 and
      v3, TLS, PKCS #5, PKCS #7, PKCS #11, PKCS #12, S/MIME, X.509 v3
      certificates, and other security standards. This is useful for
      implementing SSL and S/MIME or other Internet security standards into an
      application.
    </para>

    <bridgehead renderas="sect3">Package Information</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Homepage: <ulink url="&nss-homepage;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Wikipedia: <ulink url="&nss-wikipedia;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (HTTP): <ulink url="&nss-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download (FTP): <ulink url="&nss-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Download MD5 sum: &nss-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Download size: &nss-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated disk space required: &nss-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimated build time: &nss-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Additional Downloads</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Required patch:
          <ulink url="&patch-root;nss/nss-&nss-version;-standalone-1.patch"/>
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependencies</bridgehead>

    <bridgehead renderas="sect4">Required</bridgehead>
    <para>
      <xref linkend="nspr"/>
    </para>

    <bridgehead renderas="sect4">Recommended</bridgehead>
    <para>
      <xref linkend="sqlite"/>
    </para>

  </sect2>

  <sect2>
    <title>Installation</title>

    <note>
      <para>
        This package does not support parallel build.
      </para>
    </note>

    <para>
      Prepare the package for compilation:
    </para>

    <para>
      Compile the package:
    </para>

<screen><userinput>patch -p1 -i ../nss-&nss-version;-standalone-1.patch</userinput></screen>

<screen><userinput>cd nss &amp;&amp;
make BUILD_OPT=1 \
     NSPR_INCLUDE_DIR=/include/nspr \
     USE_SYSTEM_ZLIB=1 \
     ZLIB_LIBS=-lz \
     USE_64=1 \
     $([ -f /include/sqlite3.h ] &amp;&amp; echo NSS_USE_SYSTEM_SQLITE=1) -j1</userinput></screen>

    <para>
      Install the package:
    </para>

<screen role="root"><userinput>cd ../dist &amp;&amp;
porg -lp nss-&nss-version; "install -v -m755 Linux*/lib/*.so /lib &amp;&amp;
install -v -m644 Linux*/lib/*.chk Linux*/lib/libcrmf.a /lib &amp;&amp;
install -v -m755 -d /include/nss &amp;&amp;
cp -v -RL public/nss/* private/nss/* /include/nss &amp;&amp;
chmod -v 644 /include/nss/* &amp;&amp;
install -v -m755 Linux*/bin/certutil Linux*/bin/nss-config Linux*/bin/pk12util /bin &amp;&amp;
install -v -m644 Linux*/lib/pkgconfig/nss.pc /lib/pkgconfig"</userinput></screen>

    <sect3>
      <title>Command Explanations</title>

      <para>
        <parameter>BUILD_OPT=1</parameter>: This option is passed to
        <command>make</command> so that the build is performed with no debugging
        symbols built into the binaries and the default compiler optimizations
        are used.
      </para>

      <para>
        <parameter>NSPR_INCLUDE_DIR=/include/nspr</parameter>: This option
        sets the location of the nspr headers.
      </para>

      <para>
        <parameter>USE_SYSTEM_ZLIB=1</parameter>: This option is passed to
        <command>make</command> to ensure that the <filename
        class="libraryfile">libssl3.so</filename> library is linked to the
        system installed zlib instead of the in-tree version.
      </para>

      <para>
        <parameter>ZLIB_LIBS=-lz</parameter>: This option provides the linker
        flags needed to link to the system zlib.
      </para>

      <para>
        <parameter>USE_64=1</parameter>: This option is <emphasis>required on
        x86_64</emphasis>, otherwise <command>make</command> will try (and fail)
        to create 32-bit objects.
      </para>

      <para>
        <command>([ -f /include/sqlite3.h ] &amp;&amp; echo
        NSS_USE_SYSTEM_SQLITE=1)</command>: This tests if sqlite is installed
        and if so it <command>echo</command>s the option NSS_USE_SYSTEM_SQLITE=1
        to <command>make</command> so that <filename
        class="libraryfile">libsoftokn3.so</filename> will link against the
        system version of sqlite.
      </para>

    </sect3>

  </sect2>

</sect1>

Submitted By:            Igor Živković <contact@igor-zivkovic.from.hr>
Date:                    2015-01-31
Initial Package Version: 1.29
Upstream Status:         Not submitted
Origin:                  Self
Description:             Suppresses installation of unnecessary files.

--- libidn-1.29.orig/doc/Makefile.in
+++ libidn-1.29/doc/Makefile.in
@@ -272,8 +272,8 @@
     n|no|NO) false;; \
     *) (install-info --version) >/dev/null 2>&1;; \
   esac
-am__installdirs = "$(DESTDIR)$(infodir)" "$(DESTDIR)$(man1dir)" \
-	"$(DESTDIR)$(man3dir)" "$(DESTDIR)$(imagesdir)"
+am__installdirs = "$(DESTDIR)$(man1dir)" \
+	"$(DESTDIR)$(man3dir)"
 am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
 am__vpath_adj = case $$p in \
     $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
@@ -1929,10 +1929,10 @@
 check-am: all-am
 check: $(BUILT_SOURCES)
 	$(MAKE) $(AM_MAKEFLAGS) check-recursive
-all-am: Makefile $(INFO_DEPS) $(MANS) $(DATA)
+all-am: Makefile $(MANS)
 installdirs: installdirs-recursive
 installdirs-am:
-	for dir in "$(DESTDIR)$(infodir)" "$(DESTDIR)$(man1dir)" "$(DESTDIR)$(man3dir)" "$(DESTDIR)$(imagesdir)"; do \
+	for dir in "$(DESTDIR)$(man1dir)" "$(DESTDIR)$(man3dir)"; do \
 	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
 	done
 install: $(BUILT_SOURCES)
@@ -1988,7 +1988,7 @@
 
 info-am: $(INFO_DEPS)
 
-install-data-am: install-imagesDATA install-info-am install-man
+install-data-am: install-man
 
 install-dvi: install-dvi-recursive
 
--- libidn-1.29.orig/src/Makefile.in
+++ libidn-1.29/src/Makefile.in
@@ -196,7 +196,7 @@
 libidn_cmd_la_LINK = $(LIBTOOL) $(AM_V_lt) --tag=CC $(AM_LIBTOOLFLAGS) \
 	$(LIBTOOLFLAGS) --mode=link $(CCLD) $(libidn_cmd_la_CFLAGS) \
 	$(CFLAGS) $(AM_LDFLAGS) $(LDFLAGS) -o $@
-am__installdirs = "$(DESTDIR)$(bindir)" "$(DESTDIR)$(lispdir)"
+am__installdirs = "$(DESTDIR)$(bindir)"
 PROGRAMS = $(bin_PROGRAMS)
 am_idn_OBJECTS = idn.$(OBJEXT)
 idn_OBJECTS = $(am_idn_OBJECTS)
@@ -1411,7 +1411,7 @@
 	$(MAKE) $(AM_MAKEFLAGS) check-am
 all-am: Makefile $(LTLIBRARIES) $(PROGRAMS) $(DATA)
 installdirs:
-	for dir in "$(DESTDIR)$(bindir)" "$(DESTDIR)$(lispdir)"; do \
+	for dir in "$(DESTDIR)$(bindir)"; do \
 	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
 	done
 install: $(BUILT_SOURCES)
@@ -1470,7 +1470,7 @@
 
 info-am:
 
-install-data-am: install-dist_lispDATA
+install-data-am:
 
 install-dvi: install-dvi-am
 
